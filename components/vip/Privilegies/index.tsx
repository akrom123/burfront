import styles from './styles.module.scss'
import { FeatureCard } from '../FeatureCard'

export const Privilegies = () => {
    return <div className={styles.privilegies}>
        <FeatureCard title="Заголовок привелегии в две строки" description="'f you're just starting to place bets or use the site, then initial verification status will be enough. Initial status has lower limits than full status accounts," />
        <FeatureCard title="Заголовок привелегии в две строки" description="'f you're just starting to place bets or use the site, then initial verification status will be enough. Initial status has lower limits than full status accounts," />
        <FeatureCard title="Заголовок привелегии в две строки" description="'f you're just starting to place bets or use the site, then initial verification status will be enough. Initial status has lower limits than full status accounts," />
        <FeatureCard title="Заголовок привелегии в две строки" description="'f you're just starting to place bets or use the site, then initial verification status will be enough. Initial status has lower limits than full status accounts," />
        <FeatureCard title="Заголовок привелегии в две строки" description="'f you're just starting to place bets or use the site, then initial verification status will be enough. Initial status has lower limits than full status accounts," />
        <FeatureCard title="Заголовок привелегии в две строки" description="'f you're just starting to place bets or use the site, then initial verification status will be enough. Initial status has lower limits than full status accounts," />
    </div>
}